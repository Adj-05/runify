<h2 class="text-center mt-4">📅 Historic of my trainings</h2>

<div class="calendar-container">
  <%= month_calendar(events: @trainings) do |date, trainings| %>
    <%= date %>

    <% trainings.each do |training| %>
      <div class="training">
        <p class="fw-bold"><%= training.name %></p>
        <small>Duration : <%= training.training_duration %> min</small><br>
        <small>Genre : <%= training.music_genre %></small>
      </div>
    <% end %>
  <% end %>
</div>
